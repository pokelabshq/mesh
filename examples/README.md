# mesh examples — real-world agent workflows

each file is a complete, runnable mesh program. copy, modify, ship.

## examples/

### hello.mesh
basic pipeline — fetch, transform, output.

### monitor.mesh
service health monitoring with retry and alerting.

### pipeline.mesh
data pipeline — fetch, filter, sort, save.

### parallel.mesh
parallel fetching from multiple sources.

### digest.mesh
daily digest — aggregate data, format, notify.

### deploy.mesh
deployment workflow — build, test, deploy, notify.

### backup.mesh
backup workflow — compress, upload, verify, cleanup.

### triage.mesh
github issue triage — fetch, classify, label, notify.

### scrape.mesh
web scraping — fetch, extract, transform, save.

### report.mesh
report generation — query, aggregate, format, deliver.
